import { Typography, Box } from '@mui/material';

export function DataModels() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Data Models
      </Typography>
      <Typography variant="body1">
        This page will display the data models and their relationships.
      </Typography>
    </Box>
  );
} 